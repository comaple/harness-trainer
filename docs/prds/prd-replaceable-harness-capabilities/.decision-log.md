# Decision Log: Replaceable Harness Capabilities PRD Update

## 2026-05-31

- Input: GitHub issue `#4`, `BR-002: Replaceable Harness Capabilities`.
- Mode: `bmad-prd` update against the existing project PRD at
  `../../prd.md`.
- Decision: Do not create a second canonical PRD. The repository already uses
  `docs/prd.md` as the source of product truth.
- Decision: Treat `BR-002` as a PRD refinement issue, not as runtime
  implementation. The existing decomposition in `EPIC-002` and
  `STORY-009` through `STORY-018` remains valid.
- Change: Clarify that replaceability requires explicit public contracts, state
  ownership, failure semantics, events, replacement boundaries, and evaluator
  evidence.
- Change: Strengthen `FR-013`, `FR-014`, `FR-022`, `NFR-004`, and success
  metrics to make replacement safety reviewable.

