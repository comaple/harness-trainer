# Decision Log: Business Evaluation Mapping SPEC

## 2026-05-31

- Created `SPEC.md` for issue #34, `FR-028`, and `STORY-024` under
  `EPIC-003`.
- Used `source-extract.md` as the preservation companion for claims from issue
  #34, README, AGENTS, PRD, EPIC-003, STORY-024, architecture notes, blueprint
  template, business context intake SPEC, business environment inventory SPEC,
  and Business Agent Harness Blueprint SPEC.
- Chose Markdown-first SPEC output because the current repository uses
  Markdown as product truth and this issue asks for a BMad SPEC, not evaluator
  runtime implementation.
- Kept business evaluation mapping separate from generic replacement-safety
  evaluation because `FR-028` optimizes business-relevant target Agent outcomes
  while `FR-022` governs replaceable harness capability compatibility.

## Validation Verdict

Pass 1 coherence: the SPEC satisfies the five-field kernel, gives each
capability a stable ID with intent and success, keeps runtime and scoring
implementation choices out of the kernel, names explicit non-goals, and defines
a testable success signal for downstream evaluation mapping implementation.

Pass 2 preservation: load-bearing claims from the issue, README, AGENTS, PRD,
EPIC-003, STORY-024, architecture notes, blueprint template, context SPEC,
environment SPEC, and blueprint SPEC are preserved in `SPEC.md` or
`source-extract.md`. Wrapper-only content was intentionally excluded and
recorded in `source-extract.md`.
