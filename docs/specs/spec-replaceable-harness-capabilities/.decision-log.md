# Decision Log

## 2026-05-31

- Created `SPEC-replaceable-harness-capabilities` from GitHub issue `#4`, the
  PRD `BR-002` requirement, `EPIC-002`, and the replaceability Story set.
- Chose slug `replaceable-harness-capabilities` from the issue title.
- Created `source-extract.md` as a spec-authored companion because the issue,
  PRD, Epic, and Story extracts are load-bearing but too detailed for the
  five-field kernel.
- Self-validation pass 1: the SPEC has explicit Why, four capabilities with
  intent and success, design-bending constraints, explicit non-goals, concrete
  success signal, assumptions, and open questions.
- Self-validation pass 2: the issue requirement and acceptance criteria are
  preserved in `SPEC.md`; supporting PRD/Epic/Story claims are preserved in
  `source-extract.md`.

