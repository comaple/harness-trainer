# Decision Log: Turn Intake and Orchestration SPEC

## 2026-05-31

Created `SPEC.md` and `source-extract.md` for `FR-015` / `STORY-011` from
issue #46, PRD, EPIC-002, STORY-011, capability catalog, worker contract model,
and harness evaluator sources. Capability IDs start at `CAP-1` and are reserved
for this SPEC folder.

Self-validation verdict: coherent and preservation-complete for a
Markdown-first SPEC. The contract defines validation, persistence, state
transitions, lifecycle events, resumability, terminal states, failure behavior,
and teardown without selecting a concrete runtime framework. Open questions are
retained for downstream decisions about state-machine instance validation and
which failures remain resumable.
