# Decision Log: Session Memory and Context Compaction

## 2026-05-31

- Created `SPEC.md` from issue #56, `FR-020`, `STORY-016`, `HC-007`, the
  worker contract model, and neighboring specs.
- Kept the kernel technology-neutral: no database, queue, memory store,
  tracing backend, token counter, or compaction algorithm is selected.
- Treated memory content as references plus provenance so the contract can be
  implemented before a concrete memory store exists.
- Required compaction rejection when active task state, approval or resume
  context, queue position, branch ancestry, memory provenance, or trace context
  would be lost.
- Self-validation verdict: Spec Law passes. Every capability has intent and
  success, constraints rule out unsafe replacements and lossy compaction,
  non-goals are explicit, and source claims from the PRD, epic, story,
  capability catalog, worker contract, and neighboring specs are preserved in
  `SPEC.md` or `source-extract.md`.
