# Decision Log: Harness Training Evaluator

## 2026-05-31

- Created `SPEC.md` from issue #60, `FR-022`, `STORY-018`, `HC-009`,
  `HC-010`, the worker contract model, the existing evaluator script and
  fixtures, and neighboring specs.
- Kept the evaluator contract deterministic and dependency-free; no model call,
  hosted evaluator, telemetry backend, or subjective review step is required to
  pass CI.
- Treated `harness_eval.py` as the first concrete implementation while
  preserving the option to replace it if score dimensions, rejection semantics,
  fixture expectations, report structure, and CI behavior remain compatible.
- Preserved the boundary between generic replacement-safety evaluation and
  business-specific evaluation mapping.
- Self-validation verdict: Spec Law passes. Every capability has intent and
  success, constraints define deterministic gate behavior and replacement
  compatibility, non-goals are explicit, and source claims from the PRD, epic,
  story, capability catalog, worker contract, evaluator script, fixtures, and
  neighboring specs are preserved in `SPEC.md` or `source-extract.md`.
