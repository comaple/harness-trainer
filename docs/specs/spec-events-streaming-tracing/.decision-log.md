# Decision Log: Events Streaming and Tracing

## 2026-05-31

- Created `SPEC.md` from issue #58, `FR-021`, `STORY-017`, `HC-008`, the
  worker contract model, and neighboring specs.
- Kept the kernel technology-neutral: no event bus, stream transport, tracing
  backend, UI renderer, telemetry product, or storage runtime is selected.
- Required stable run, turn, session, message, function/tool, event, trace, and
  span identifiers so token streams, UI events, automation events, and traces
  can be correlated.
- Treated audit event loss and missing trace context as explicit failures,
  with observability-degraded behavior for unrecoverable trace failures.
- Self-validation verdict: Spec Law passes. Every capability has intent and
  success, constraints protect replay and replacement compatibility, non-goals
  are explicit, and source claims from the PRD, epic, story, capability
  catalog, worker contract, and neighboring specs are preserved in `SPEC.md`
  or `source-extract.md`.
